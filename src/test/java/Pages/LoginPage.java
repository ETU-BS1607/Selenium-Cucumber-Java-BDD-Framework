package Pages;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;

public class LoginPage {

	/*
	 * WebDriver driver;
	 * 
	 * // Constructor to initialize the WebDriver public LoginPage(WebDriver driver)
	 * { this.driver = driver; }
	 * 
	 * // Locators By usernameField = By.id("username"); By passwordField =
	 * By.id("password"); By loginButton = By.id("submit");
	 * 
	 * // Method to enter username public void enterUsername(String username) {
	 * driver.findElement(usernameField).sendKeys(username); }
	 * 
	 * // Method to enter password public void enterPassword(String password) {
	 * driver.findElement(passwordField).sendKeys(password); }
	 * 
	 * // Method to click login button public void clickLoginButton() {
	 * driver.findElement(loginButton).click(); }
	 * 
	 * // Method to verify if login is successful public boolean isLoginSuccessful()
	 * { return driver.getPageSource().contains("Logged In Successfully"); }
	 */
}
